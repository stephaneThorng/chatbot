intent_training:
  model_name: distilbert-base-multilingual-cased
  num_epochs: 8
  batch_size: 8
  learning_rate: 3.0e-5
  warmup_steps: 100
  weight_decay: 0.01

ner_training:
  model_name: distilbert-base-multilingual-cased
  num_epochs: 8
  batch_size: 8
  learning_rate: 3.0e-5
  warmup_steps: 100
  weight_decay: 0.01
  weight_decay: 0.01
